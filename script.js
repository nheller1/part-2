let rain = prompt ("How many inches of rain fell?");
let inches = "*";
console.log("Rainfall: " + inches.repeat(rain));

let fertilizer = prompt("Did you use fertilizer?");
if (fertilizer ==="no"){
    (inches<= light rain);
  console.log("The yield should be " + (50*.8) + " per year.");
}
if (fertilizer ==="no") {
    (inches <= heavyrain)
      console.log(The yield should be" + (50.9) + "per year.")
  
  
  console.log("the yield should be" +BLANK+ "per year.");
}

if (fertilizer === "yes") {
  
  let brand = prompt("Did you use premium or regular fertilizer?");
    if(fertilizerType === "premium") {
        result+= (The yield should be 51.75 bushels per acre.)
  
  
}
console.log(result);
